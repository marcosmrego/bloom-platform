DB_HOST=postgres
DB_PORT=5432
DB_NAME=bloom
DB_USER=postgres
DB_PASSWORD=changeme
CONTENT_API_TOKEN=
CONTENT_AUTOPUBLISH_ENABLED=false

# Frontend / runtime
BLOOM_API=http://localhost:8001
# Use somente para instância dedicada. Deixe vazio em deploy multi-domínio.
BLOOM_TENANT=viralbarato
PORT=3000

# Monetização do frontend (configure separadamente em cada instância)
ADSENSE_CLIENT_ID=
ADCASH_HEAD_CODE=
ADCASH_BODY_CODE=
ADS_TXT_EXTRA=

# Optional: override these for remote DB or production
# DB_HOST=postgres.example.internal
# DB_PASSWORD=production_password_here
